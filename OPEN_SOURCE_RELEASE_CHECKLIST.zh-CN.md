## 开源发布检查清单

在将仓库公开或发布新版本前，请按下面清单逐项检查。

### 仓库整洁性

- 确认 `LICENSE` 已存在且与计划使用的许可证一致，即 `GPLv3`
- 确认 `README.md` 已反映当前功能、许可证和法律声明
- 确认 `THIRD_PARTY_NOTICES.zh-CN.md` 或对应英文说明已更新到最新状态
- 确认 `.gitignore` 已排除本机配置、导入资源、日志和构建产物
- 确认没有将本地测试数据、缓存文件或生成文件纳入提交

### 敏感信息检查

- 搜索本机绝对路径，例如 `C:\Users\...`
- 搜索 `steam_path`、本地 URL、调试日志、崩溃报告
- 搜索任何 `token`、`password`、`cookie`、`key`、`webhook`
- 搜索更新接口地址，并确认这些地址本就打算公开

### 资源与素材检查

- 删除任何未获得明确再分发授权的媒体文件
- 删除包含第三方素材的导入资源包、示例配置和本地预设
- 删除你无法确认授权来源的缩略图、预览图、图标和字体
- 确认仓库内的图片、音频、视频和字体要么是你自制、要么有明确授权、要么不随仓库分发

### 品牌与法律边界

- 明确声明项目与 `Valve` 无官方关联
- 避免捆绑 `Valve` 或 `Counter-Strike 2` 的专有资源
- 避免在仓库资源中使用未经许可的第三方品牌 Logo
- 明确区分“代码许可证”与“品牌/图标/截图/赞赏码/默认宣传素材”的授权边界
- 确认 `README.md` 已说明“用户自行导入资源的版权责任由用户承担”

### 依赖许可证复核

- 按 `requirements.txt` 重新检查实际使用版本的许可证
- 保留所有需要附带的第三方许可证和声明
- 确认二进制发布方式符合所打包依赖的许可证要求
- 特别确认 `PySide6-Fluent-Widgets` 对当前分发模式是否构成额外限制

### 发布包检查

- 不要只发布 `dist/` 产物而不按 `GPLv3` 提供对应源码
- 不要发布包含个人路径、本机配置或调试痕迹的测试构建
- 确认发布说明中没有“官方”“绝对安全”“绝不封号”之类无法保证的表述

### 最终 Git 检查

- 运行 `git status --short`
- 手动检查每一个已暂存文件
- 确认 `imported/`、`configs/`、`thumbnails/`、`dist/`、`*.log` 没有被纳入提交
- 确认没有任何崩溃报告、调试日志或用户本地配置进入仓库
