## 第三方许可证说明

本文档用于概述本项目所使用的重要第三方依赖及其许可证边界。

本文档仅作信息说明，不替代各上游项目发布的原始许可证文本。
在分发源码或二进制版本前，你仍应根据实际使用的依赖版本复核对应许可证，并保留所需声明。

### 项目许可证

- `CS2Toolkit`：`GPL-3.0-only`
- 在继续使用 `PySide6-Fluent-Widgets` 且未取得其商业许可证的前提下，本项目当前开源分发应理解为走 `GPLv3` 路线，而不是“可额外附加禁止修改、禁止再分发、禁止收费分发”等限制的自定义许可路线

### 运行时依赖

- `PySide6`
  - 上游：Qt for Python
  - 许可证摘要：开源分发通常基于 `LGPL-3.0-only OR GPL-2.0-only OR GPL-3.0-only`
  - 说明：`PySide6` 自身可以在其许可框架下用于商业分发，但这并不代表依赖集合中的其他库也自动允许整个应用直接闭源商用。

- `PySide6-Fluent-Widgets`
  - 上游：`zhiyiYo / atiasn`
  - 许可证摘要：上游项目声明其对非商业/开源使用采用 `GPLv3`，同时另行提供商业许可证
  - 说明：这是本项目当前最关键的许可证约束来源。如果你将本项目按 `GPLv3` 开源发布，通常与该依赖的开源使用方式是匹配的；如果你想做闭源分发、单独商业发行，或希望对代码增加与 `GPLv3` 冲突的额外限制，则不能只依据 `PySide6` 的可商用性判断，通常应单独向该库作者获取商业许可证，或移除该依赖后重新评估整项目许可策略。

- `requests`
  - 上游：Python Software Foundation 生态
  - 许可证摘要：`Apache-2.0`

- `pycaw`
  - 上游：AndreMiras
  - 许可证摘要：`MIT`

- `zstandard`
  - 上游：Gregory Szorc
  - 许可证摘要：上游仓库声明为 `BSD-3-Clause`

### 平台与外部接口

- `Steam` 注册表与安装路径检测仅用于本地发现用户游戏安装位置。
- `Counter-Strike 2` 的 `Game State Integration`（`GSI`）属于游戏提供的本地接口。
- `Valve`、`Counter-Strike`、`Counter-Strike 2` 及其相关名称、图形、商标和资源，不属于本项目许可证授予范围。

### 分发注意事项

- 在 `GPLv3` 下，第三方可以修改、再分发，甚至收费分发项目代码，但分发方仍需继续遵守 `GPLv3` 的开源义务。
- 因此，`GPLv3` 可以约束“闭源分发”，但不能阻止“在继续开源并遵守许可证前提下的再分发或收费分发”。
- 不要因为代码“可以加载”某个媒体文件，就默认该媒体文件可以再分发。
- 不要捆绑游戏原始资源、动漫或游戏角色图片、音效包、字体包、以及用户自行导入的资源，除非你明确拥有再分发授权。
- 对于本项目，应从“整套依赖组合”角度判断许可证边界，而不是只看 `PySide6`；`PySide6-Fluent-Widgets` 会实质性影响商业分发方式。
- 项目名称、图标、Logo、截图、赞赏码图片、默认宣传素材以及其他未单独声明授权的非代码资源，不应默认视为 `GPLv3` 代码授权的一部分。
- 如果你基于本项目进行 fork、修改或再分发，不应暗示你的版本属于原作者官方版本，也不应未经许可继续使用原作者的品牌标识、作者身份信息或赞赏相关素材。
- 如果你要发布二进制版本，建议至少附带：
  - 项目的 `LICENSE`
  - 本文档 `THIRD_PARTY_NOTICES.zh-CN.md`
  - 与实际打包依赖版本对应的其他必要许可证或声明文件

### 复核来源

在正式公开发布前，请重新检查你当前实际使用版本对应的上游许可证页面：

- `PySide6`：PyPI / Qt for Python 官方文档
- `PySide6-Fluent-Widgets`：上游 GitHub / PyPI 页面
- `requests`：PyPI / 上游 GitHub 许可证
- `pycaw`：上游 GitHub 许可证
- `zstandard`：PyPI / 上游 GitHub 许可证
